# Resilient installs on flaky/constrained networks (e.g. some EC2 instances).
# Fewer parallel connections = fewer "error (23)" registry failures; more retries.
network-concurrency=4
child-concurrency=2
fetch-retries=6
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=90000
fetch-timeout=180000
